'use client';
import Link from 'next/link';
import {usePathname} from 'next/navigation';
import {useState} from 'react';
import {ArrowUpRight, Menu, X} from 'lucide-react';
export const pages=[['Home','/'],['About','/about'],['What We Do','/what-we-do'],['Ideas & Ventures','/ideas-ventures'],['Technology','/technology'],['Insights','/insights'],['Partner With Us','/partner-with-us'],['Contact','/contact']];
export function SiteHeader(){const path=usePathname();const [open,setOpen]=useState(false);return <header className="header"><Link href="/" className="brand" aria-label="Techsavvy Ideas home">techsavvy<span>ideas<span className="brand-dot">.</span></span></Link><button className="menu-button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="navigation" aria-label={open?'Close navigation':'Open navigation'}>{open?<X/>:<Menu/>}</button><nav id="navigation" className={open?'navigation open':'navigation'} aria-label="Main navigation">{pages.map(([title,url])=><Link key={url} href={url} onClick={()=>setOpen(false)} aria-current={path===url?'page':undefined} className={url==='/contact'?'nav-contact':''}>{title}{url==='/contact'&&<ArrowUpRight size={14}/>}</Link>)}</nav></header>}
export function SiteFooter(){return <footer><div className="footer-grid"><div><Link href="/" className="brand">techsavvy<span>ideas<span className="brand-dot">.</span></span></Link><h3>Ideas. Technology. Solutions.</h3><p>Conceiving, building and commercializing technology-driven solutions for a better future.</p></div><div><h4>Company</h4>{pages.slice(1,5).map(([t,u])=><Link href={u} key={u}>{t}</Link>)}</div><div><h4>Explore</h4>{pages.slice(5).map(([t,u])=><Link href={u} key={u}>{t}</Link>)}</div><div><h4>Connect</h4><p className="small">Social channels coming soon.</p>{['LinkedIn','X','Facebook','Instagram'].map(t=><span className="unavailable" key={t}>{t}</span>)}<Link href="/contact">Email enquiries <ArrowUpRight size={13}/></Link></div></div><div className="footer-bottom"><span>© 2026 Techsavvy Ideas Limited. All rights reserved.</span><span>From possibility to progress.</span></div></footer>}
